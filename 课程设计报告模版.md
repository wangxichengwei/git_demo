# 课程设计报告

**项目名称**：Project 3: [带有约束的推车倒立摆轨迹优化]  
（例如：Project 1: 双摆系统的能量成型与 LQR 切换控制 (Acrobot Swing-up and Balance)）

**学生姓名**：王伟晨  
**学号**：2024312382  
**完成日期**：2026 年 5 月 23 日

---

## 摘要  
<div style="font-size: 16px;">
本项目针对欠驱动推车倒立摆系统，在推车行程受限、控制力饱和等实际约束下，实现了从静止下垂到竖直平衡目标位置的最优轨迹生成与鲁棒跟踪控制。首先，利用拉格朗日方程建立精确的非线性动力学模型。其次，采用直接转录法将连续时间最优控制问题离散化为非线性规划问题，在 CasADi + IPOPT 框架中求解满足位置、控制力约束及终端平衡态约束的最小能量轨迹。随后，基于该标称轨迹设计时变线性二次型调节器 (TVLQR)，通过对沿轨迹的线性时变系统求解 Riccati 方程获得反馈增益，实现闭环扰动抑制。仿真结果表明，直接转录法可生成平滑且严格满足约束的 S 形位置-角度协同轨迹；TVLQR 控制器能够在 0.05 rad 初始摆角扰动下快速收敛至标称轨迹，跟踪误差小于 0.01 rad。消融实验表明，控制代价矩阵 R 的选取显著影响恢复速度与控制峰值：过小的 R 导致控制饱和并诱发振荡，过大 R 则使恢复缓慢。此外，分析了约束冲突导致的优化失败案例，验证了终端加速度约束的必要性。
**关键词**：倒立摆；直接转录法；轨迹优化；时变 LQR；约束处理；欠驱动系统
---

## 1. 引言

### 1.1 项目背景与意义
推车倒立摆是经典欠驱动系统，其控制问题是验证非线性控制与轨迹优化算法的基准。实际应用中，推车行程常受轨道长度或安全区域限制，驱动器输出力矩有限，这要求控制器不仅实现平衡与位置转移，还需严格满足状态与控制约束。轨迹优化方法能够直接处理这些硬约束，生成开环最优轨线；结合时变 LQR 则赋予系统对初始误差和外界扰动的鲁棒性。本项目的核心任务即为：设计满足约束的优化轨迹，并实现可靠跟踪。
### 1.2 相关研究现状
直接转录法在轨迹优化中被广泛采用，其将连续状态与控制离散化，转化为 NLP 问题求解（Betts, 2010）。Tedrake (2022) 的《Underactuated Robotics》课程中详细介绍了针对倒立摆的轨迹优化与 TVLQR 方法，并提供了开源代码库。本报告参考其框架，结合 CasADi 实现高效建模与求解。
### 1.3 本报告结构安排
第 2 节推导系统动力学模型，第 3 节阐述直接转录法与 TVLQR 控制器设计，第 4 节说明仿真实现，第 5 节展示实验结果、消融实验及失败案例分析，第 6 节总结结论。

---

## 2. 系统建模与动力学推导


### 2.1 系统描述（简图 + 参数表）
系统由质量为 \(M\) 的小车和长为 \(l\)、质量为 \(m\) 的均质摆杆组成，如图所示。小车在水平方向运动，控制输入为作用在小车上的水平力 \(u\)。广义坐标取小车位移 \(x\) 和摆杆相对竖直向上方向的偏角 \(\theta\)（顺时针为正）。

**表 2-1 系统物理参数**

| 参数 | 符号 | 数值 | 单位 | 选取依据 |
|------|------|------|------|----------|
| 小车质量 | \(M\) | 1.0 | kg | 典型实验台参数 |
| 摆杆质量 | \(m\) | 0.1 | kg | 轻杆假设 |
| 杆半长 (质心到转轴) | \(l\) | 0.5 | m | 适中尺寸 |
| 重力加速度 | \(g\) | 9.81 | m/s² | 标准值 |
| 转动惯量 | \(I\) | \(\frac{1}{3}ml^2\) | kg·m² | 均质杆绕端点 |

### 2.2 拉格朗日建模
系统动能：小车动能 \(T_c = \frac{1}{2}M\dot{x}^2\)。摆杆质心坐标 \(x_m = x + l\sin\theta, y_m = l\cos\theta\)，速度平方：
\[
\dot{x}_m^2 + \dot{y}_m^2 = (\dot{x} + l\dot{\theta}\cos\theta)^2 + (-l\dot{\theta}\sin\theta)^2 = \dot{x}^2 + l^2\dot{\theta}^2 + 2l\dot{x}\dot{\theta}\cos\theta
\]
摆杆动能（平动+转动）：\(T_p = \frac{1}{2}m(\dot{x}_m^2+\dot{y}_m^2) + \frac{1}{2}I\dot{\theta}^2 = \frac{1}{2}m(\dot{x}^2 + l^2\dot{\theta}^2 + 2l\dot{x}\dot{\theta}\cos\theta) + \frac{1}{2}I\dot{\theta}^2\)。
总动能：
\[
T = \frac{1}{2}(M+m)\dot{x}^2 + m l \dot{x}\dot{\theta}\cos\theta + \frac{1}{2}(I+ml^2)\dot{\theta}^2
\]
势能（以小车质心为零点）：\(V = m g l \cos\theta\)。
<div style="font-size: 16px;">
拉格朗日量 \(L = T - V\)，欧拉-拉格朗日方程：
\[
\frac{d}{dt}\frac{\partial L}{\partial \dot{x}} - \frac{\partial L}{\partial x} = u, \quad \frac{d}{dt}\frac{\partial L}{\partial \dot{\theta}} - \frac{\partial L}{\partial \theta} = 0
\]
代入计算得：
\[
(M+m)\ddot{x} + m l \ddot{\theta}\cos\theta - m l \dot{\theta}^2\sin\theta = u
\]
\[
m l \ddot{x}\cos\theta + (I+ml^2)\ddot{\theta} - m g l \sin\theta = 0
\]
令 \(\beta = I+ml^2 = \frac{4}{3}ml^2\)，解耦出 \(\ddot{x}\) 与 \(\ddot{\theta}\)：
\[
\ddot{x} = \frac{u + m l \dot{\theta}^2\sin\theta - \frac{m^2 l^2 g}{\beta}\sin\theta\cos\theta}{M+m - \frac{m^2 l^2}{\beta}\cos^2\theta}
\]
\[
\ddot{\theta} = \frac{m l}{\beta}(g\sin\theta - \ddot{x}\cos\theta)
\]
定义状态向量 \(\mathbf{z} = [x,\theta,\dot{x},\dot{\theta}]^\top\)，则状态空间模型为 \(\dot{\mathbf{z}} = f(\mathbf{z},u)\)。

---

## 3. 控制器 / 算法设计

### 3.1 直接转录轨迹优化
将时间域 \([0,T]\) 等分为 \(N\) 段，步长 \(\Delta t = T/N\)。状态节点 \(\mathbf{z}_0,\dots,\mathbf{z}_N\)，控制输入采用零阶保持，每个区间取常值 \(u_k\) (\(k=0,\dots,N-1\))。动力学约束采用梯形配点法：
\[
\mathbf{z}_{k+1} - \mathbf{z}_k = \frac{\Delta t}{2}[f(\mathbf{z}_k,u_k) + f(\mathbf{z}_{k+1},u_k)]
\]
目标函数为最小化控制能量：
\[
J = \int_0^T u^2(t)dt \approx \Delta t \sum_{k=0}^{N-1} u_k^2
\]
边界条件：起点 \(\mathbf{z}_0 = [0,0,0,0]^\top\)，终点 \(\mathbf{z}_N = [2.0,0,0,0]^\top\)。额外增加终点加速度约束，保证到达后为真正的平衡态（无需控制力维持）：
\[
\ddot{x}_N = 0,\quad \ddot{\theta}_N = 0
\]
硬约束：
- 推车位置：\(-1.5 \le x_k \le 3.0\) m
- 控制力：\(|u_k| \le 20\) N

### 3.2 时变 LQR 跟踪控制器
给定标称轨迹 \(\bar{\mathbf{z}}(t), \bar{u}(t)\)，定义误差状态 \(\delta\mathbf{z} = \mathbf{z} - \bar{\mathbf{z}}\) 和误差输入 \(\delta u = u - \bar{u}\)。沿标称轨迹线性化得到时变线性系统：
\[
\delta\dot{\mathbf{z}} = \mathbf{A}(t)\delta\mathbf{z} + \mathbf{B}(t)\delta u
\]
式中 \(\mathbf{A}(t) = \frac{\partial f}{\partial \mathbf{z}}\big|_{\bar{\mathbf{z}},\bar{u}},\ \mathbf{B}(t) = \frac{\partial f}{\partial u}\big|_{\bar{\mathbf{z}},\bar{u}}\)。使用数值微分计算 Jacobian。
以控制周期 \(dt_{\text{lqr}}=0.01\) s 离散化，采用矩阵指数方法得到离散系统矩阵 \(\mathbf{A}_{d,k}, \mathbf{B}_{d,k}\)。
时变 LQR 代价函数：
\[
J_{\text{lqr}} = \sum_{k=0}^{N_{\text{lqr}}-1} (\delta\mathbf{z}_k^\top \mathbf{Q} \delta\mathbf{z}_k + \delta u_k^\top \mathbf{R} \delta u_k) + \delta\mathbf{z}_{N_{\text{lqr}}}^\top \mathbf{Q}_f \delta\mathbf{z}_{N_{\text{lqr}}}
\]
权重矩阵：\(\mathbf{Q} = \mathrm{diag}(10, 100, 1, 10)\)，\(\mathbf{R}=0.1\)，\(\mathbf{Q}_f = \mathbf{Q}\)。逆向 Riccati 方程：
\[
\mathbf{P}_N = \mathbf{Q}_f
\]
\[
\mathbf{K}_k = (\mathbf{R} + \mathbf{B}_{d,k}^\top \mathbf{P}_{k+1} \mathbf{B}_{d,k})^{-1} \mathbf{B}_{d,k}^\top \mathbf{P}_{k+1} \mathbf{A}_{d,k}
\]
\[
\mathbf{P}_k = \mathbf{Q} + \mathbf{A}_{d,k}^\top \mathbf{P}_{k+1} \mathbf{A}_{d,k} - \mathbf{K}_k^\top (\mathbf{R} + \mathbf{B}_{d,k}^\top \mathbf{P}_{k+1} \mathbf{B}_{d,k}) \mathbf{K}_k
\]
控制律：\(u = \bar{u}(t) - \mathbf{K}(t)(\mathbf{z} - \bar{\mathbf{z}}(t))\)，并限幅在 \([-20, 20]\) N。

### 3.3 参数整定依据
- 优化时长 \(T=3\) s，节点数 \(N=50\)，平衡精度与计算量。
- 位置约束依据假设轨道长度，目标位置 \(2.0\) m 在约束范围内。
- LQR 权重：\(\mathbf{Q}\) 中对 \(\theta\) 权重大，优先稳定摆角；\(\mathbf{R}=0.1\) 适中控制量。
- 消融实验中改变 \(\mathbf{R}\) 观察对性能影响。


---

## 4. 仿真实现

### 4.1 仿真平台与工具包
（Python + matplotlib / scipy / control / casadi / pydrake 等）
### 4.2 代码结构说明（主要函数 / 类）

1. f_expr(z, u)

功能：CasADi 符号形式的倒立摆动力学方程
输入：z(4 维状态向量)、u(控制输入)
输出：4 维状态导数向量 [dx, dtheta, ddx, ddtheta]
用途：专门用于轨迹优化的符号计算，支持自动微分与约束构建

2. f_numeric(z, u)

功能：NumPy 数值形式的倒立摆动力学方程
输入：z(4 维数组)、u(标量控制)
输出：4 维数值状态导数数组
用途：用于闭环仿真的数值积分，是系统仿真的核心

3. discretize_AB(Ac, Bc, dt)

功能：连续时间系统矩阵 → 离散时间系统矩阵
输入：连续状态矩阵Ac、连续输入矩阵Bc、采样时间dt
输出：离散状态矩阵Ad、离散输入矩阵Bd
用途：将线性化后的连续系统转化为离散形式，适配 LQR 设计

### 4.3 完整动画演示描述（可附 GIF 或视频链接）
<div style="font-size: 16px;">
动画展示小车在轨道上运动及摆杆摆动，标称轨迹与实际轨迹可叠加颜色区分。动画帧率约为 20 fps，时长 3 s。

https://github.com/wangxichengwei/git_demo
---

## 5. 实验结果与分析

### 5.1 典型成功轨迹
图5-1
![alt text](image.png)
图5-2
![alt text](image-1.png)
图5-3
![alt text](image-2.png)
图5-4
![alt text](image-3.png)

- **图 5-1 位置与摆角曲线**：小车从 0 m 先小幅后退至约 -0.2 m，随后平滑加速向右，约 2 s 后减速，最终精确停在 2.0 m，全程未触及位置约束边界。摆角峰值约 ±0.15 rad，后期收敛至 0。控制力呈先负后正再负的阶梯状，最大幅值约 10 N，远未饱和，终点附近控制力趋于 0，验证加速度约束生效。
- **图 5-2 跟踪对比（TVLQR 闭环，初始扰动 \(\theta_0=0.05\) rad）**：实际位置与摆角紧密跟随标称值，角度误差在 0.5 s 内收敛至 0.01 rad 以内；控制输入在初始阶段有大幅修正，之后与标称曲线重合。

### 5.2 消融实验：R 矩阵对 LQR 性能的影响
保持 \(\mathbf{Q}\) 不变，取 \(\mathbf{R} = 0.01, 0.1, 1.0\) 三组，仿真初始摆角扰动 0.05 rad，结果见表 5-1 与图 5-4。

**表 5-1 R 矩阵消融实验对比**

| 实验组 | \(\mathbf{R}\) | 角度误差收敛时间 (s) | 最大控制力 (N) | 稳态角度误差 (rad) | 备注 |
|--------|----------------|----------------------|----------------|---------------------|------|
| 基准   | 0.1            | 0.5                  | 14.2           | <0.005              | 平稳恢复 |
| 小 R   | 0.01           | 0.25                 | 20 (饱和)      | <0.005              | 控制高频振荡，饱和频繁 |
| 大 R   | 1.0            | 1.2                  | 8.3            | 0.02                | 恢复缓慢，存在静差 |

**分析**：
- 减小 R 使控制器更激进，控制增益升高，扰动抑制更快，但容易触碰饱和上限（20 N），实际执行中因限幅导致非线性效应，出现控制量高频切换，工程上不可取。
- 增大 R 使控制代价敏感，增益降低，角度恢复慢，且由于模型线性化误差，稳态误差稍大。
- 基准组 \(R=0.1\) 在快速性与控制平滑性之间取得良好平衡。

### 5.3 失败案例分析
#### 案例 1：位置约束冲突导致优化不可行
若将目标终点 \(x_f=2.0\)，而位置约束设为 \(x_{\max}=1.5\) m，则约束直接矛盾，IPOPT 报告 `infeasible`。优化器无法找到满足终端与路径约束的解。改进措施：合理设置约束范围，确保目标点在允许域内。

#### 案例 2：忘记添加终端加速度约束
最初代码仅约束终点状态，未约束 \(\ddot{x}=0\)，导致优化结果为满足离散方程而在终端保留了约 2.5 N 的控制力。此时名义上终点速度为零，但小车加速度非零，实际不能维持静止。添加 `opti.subject_to( f_end[2]==0 )` 后控制力归零，位置曲线也不再出现“触碰上限后回落”的现象。这凸显了在离散优化中显式约束终端加速度的必要性。

---

## 6. 结论与展望

### 6.1 主要工作总结
成功构建推车倒立摆非线性模型，利用直接转录法生成了满足行程、控制约束及终端平衡态的最优轨迹。在此基础上设计的 TVLQR 控制器能有效抑制初始扰动，实现高精度轨迹跟踪。

### 6.2 性能指标
- 标称轨迹：控制能量 \( \int u^2 dt \approx 45.2 \)，终端状态误差 \(<10^{-8}\)。
- TVLQR 跟踪：0.05 rad 扰动下角度误差 0.5 s 内收敛至 0.01 rad，位置误差 <0.005 m。

### 6.3 不足之处
- TVLQR 依赖标称轨迹精度，若轨迹过于接近约束边界，线性化假设可能失效。
- 未考虑模型参数不确定性或测量噪声，实际应用需进一步鲁棒设计。

### 6.4 未来工作
- 引入鲁棒 MPC 或迭代 LQR 应对非线性与大扰动。
- 在硬件实验台验证控制策略。

---

## 参考文献

[1] Betts, J. T. (2010). *Practical Methods for Optimal Control and Estimation Using Nonlinear Programming*. SIAM.  
[2] Tedrake, R. (2022). *Underactuated Robotics: Algorithms for Walking, Running, Swimming, Flying, and Manipulation*. MIT OpenCourseWare.  
[3] Andersson, J. A. E., et al. (2019). CasADi – A software framework for nonlinear optimization and optimal control. *Mathematical Programming Computation*, 11(1), 1–36.

---

## 附录

### A.1 完整代码（或 GitHub 链接）
https://github.com/wangxichengwei/git_demo
### A.2 额外图表 / 数据
### A.3 关键代码片段（Lagrangian、LQR 增益、Value Iteration 核心循环、优化问题等）
A.3.1 CasADi 符号动力学（用于优化）

def f_expr(z, u):
    x, theta, dx, dtheta = z[0], z[1], z[2], z[3]
    sin_theta = ca.sin(theta)
    cos_theta = ca.cos(theta)
    tmp = (I + m*l**2)
    denom = M + m - (m**2 * l**2 * cos_theta**2) / tmp
    num = u + m*l * dtheta**2 * sin_theta - (m**2 * l**2 * g * sin_theta * cos_theta) / tmp
    ddx = num / denom
    ddtheta = (m*l * (g * sin_theta - ddx * cos_theta)) / tmp
    return ca.vertcat(dx, dtheta, ddx, ddtheta)

A.3.2 直接转录优化问题构建（CasADi Opti）

opti = ca.Opti()
Z = opti.variable(4, N+1)   # 状态变量
U = opti.variable(1, N)     # 控制变量

目标函数：最小化控制能量
cost = ca.sum2(U**2) * dt
opti.minimize(cost)
//梯形配点动力学约束
for k in range(N):
    zk, zk1, uk = Z[:,k], Z[:,k+1], U[:,k]
    fk = f_expr(zk, uk)
    fk1 = f_expr(zk1, uk)
    opti.subject_to( zk1 - zk == (dt/2) * (fk + fk1) )
//边界条件与终端加速度约束
opti.subject_to( Z[:,0] == [0,0,0,0] )
opti.subject_to( Z[:,-1] == [2.0,0,0,0] )
f_end = f_expr(Z[:,-1], U[:,-1])
opti.subject_to( f_end[2] == 0 )  # 终端横向加速度=0
opti.subject_to( f_end[3] == 0 )  # 终端角加速度=0
//硬约束
opti.subject_to( opti.bounded(-1.5, Z[0,:], 3.0) )   # 位置
opti.subject_to( opti.bounded(-20.0, U, 20.0) )       # 控制力
opti.solver('ipopt')
sol = opti.solve()

A.3.3 时变 LQR 增益逆向计算

//离散 Riccati 逆向迭代
P = Qf
K_gains = [None] * N_lqr
for k in reversed(range(N_lqr)):
    Ad = A_d_list[k]
    Bd = B_d_list[k]
    # 计算当前步最优增益
    K = np.linalg.inv(R + Bd.T @ P @ Bd) @ (Bd.T @ P @ Ad)
    K_gains[k] = K
    # 更新 Riccati 矩阵
    P = Q + Ad.T @ P @ Ad - K.T @ (R + Bd.T @ P @ Bd) @ K

A.3.4 闭环仿真中的 TVLQR 控制律

for step in range(N_lqr):
    z_nom = interp_z(t_lqr[step])
    u_nom = interp_u(t_lqr[step])
    delta_z = z_sim - z_nom
    u = float(u_nom - K_gains[step] @ delta_z)
    u = np.clip(u, -u_max, u_max)   # 控制限幅
    # 欧拉积分推进仿真
    dz = f_numeric(z_sim, u)
    z_sim = z_sim + dz * dt_lqr

---

**提交 checklist（请自行核对）**
- [ ] 报告 PDF（含所有图表、公式、表格）
- [ ] 代码文件（`.ipynb` 或 `.py`，必须能直接运行并显示动画）
- [ ] 至少 1 组消融/对比实验
- [ ] 失败案例分析
- [ ] 所有图表有编号和图注
